# 今天要做的事
- JS复习
- C++复习
- 阅读论文，实质性的内容积累
- 数据结构线性表顺序存储结构的代码实现
# 完成情况

- 完成复习
  - 使用原型对象prototype( )，添加构造函数创建的类中共有的属性和方法
  - 垃圾回收操作
    - 对象不需要使用时直接让其在堆区的内存为空null，以便回收

  - 数组
    - 增删改查操作
    - 遍历
    - 数组的元素可以是任意数据类型

- new关键字在堆区开辟内存

  - 为什么delete释放的内存还能访问到？是编译器的原因？

  - 释放堆区开辟的数组的内存记得加[ ]：delete[ ] arr;

- 引用

  - 引用的本质：指针常量，指针指向不可以修改，指针指向的值可以修改，发现常量指针const加在数据类型前和加在*前是一样的
  - 引用做函数参数传递，效果和地址传递一样，代码更清晰
  - 引用做函数返回值不太理解

  - 常量引用，引用做函数参数时可以使用常量引用防止误操作形参

- 函数重载
  - 函数重载发生的条件

- 封装
  - class和struct的区别：默认的访问权限不同
    - class的默认访问权限是私有权限private
    - struct的默认访问权限是公共权限public

- 对象的初始化和清理——构造函数和析构函数

  - 构造函数的分类及调用

  - 析构函数每调用一次构造函数就会自动调用一次

  - 拷贝构造函数

    - 怎样创建一个拷贝构造函数const Person &p
    - 怎样调用拷贝构造函数

    - 什么时候会调用拷贝构造函数
      - 使用一个已经创建完毕的对象初始化一个新对象时
      - 使用对象作为函数参数，值传递的方式给函数参数传值时（只要创建的对象作为函数参数传递，那么就会调用拷贝构造函数）
      - 以值方式返回局部对象时（不理解）

- 未完成
- 需要学习抽象数据类型ADT

